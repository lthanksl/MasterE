<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="emp">
  
  <select id="all" resultType="mybatis.vo.EmpVO">
  	SELECT * FROM employees
  </select>
  
  <select id="Search1" parameterType="java.util.Map" resultType="mybatis.vo.EmpVO">

		SELECT * FROM EMPLOYEES 
		<if test="searchType != null and searchValue != null">
			<trim prefix="where">
				<choose>
					<when test="searchType == 0">
						LOWER(FIRST_NAME) Like LOWER(#{searchValue})
						
					</when>
					<when test="searchType == 1">
						UPPER(JOB_ID) Like UPPER(#{searchValue})
					</when>
					<when test="searchType == 2">
						EMPLOYEE_ID Like #{searchValue}
					</when>
					<when test="searchType == 3">
						 DEPARTMENT_ID Like #{searchValue}
					</when>
				</choose>
			</trim>
		</if>
		
	</select>
	
	<!-- LOWER(FIRST_NAME) Like LOWER('%'||#{searchValue} || '%') -->
   <select id="searchName" parameterType="java.util.Map"
  resultType="mybatis.vo.EmpVO">
  	
  	SELECT * FROM EMPLOYEES 	
		<if test="searchType != null and searchValue != null">
			<trim prefix="where">
				<choose>
					<when test="searchType == 0">
						LOWER(FIRST_NAME) Like LOWER(#{searchValue})
						
					</when>
					<when test="searchType == 1">
						UPPER(JOB_ID) Like UPPER(#{searchValue})
					</when>
					<when test="searchType == 2">
						EMPLOYEE_ID Like #{searchValue}
					</when>
					<when test="searchType == 3">
						 DEPARTMENT_ID Like #{searchValue}
					</when>
				</choose>
			</trim>
		</if>
  </select>
  
</mapper>